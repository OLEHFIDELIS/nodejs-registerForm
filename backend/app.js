const express = require("express");

app.get("/", (req,res) => {
    res.render()
});

app.post("/", (req,res) => {
    req.body

})



app.listen("3000", () => {
    console.log("YelpCamp Server Just Started");
});